name: Build ZMK firmware
on: [push]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
    strategy:
      matrix:
        build:
          - board: nrf52840dongle_nrf52840
            shield: mykb
            keymap: config/mykb.keymap
