name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    strategy:
      fail-fast: false
      matrix:
        board:
          - mykb
    with:
      board: ${{ matrix.board }}
